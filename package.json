{
  "name": "@datarecs/github-actions-auth",
  "version": "1.0.0",
  "description": "Exchange GitHub Actions OIDC token for a DataRecs access token",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "ncc build src/index.ts -o dist",
    "pretest": "npm run build",
    "test": "node --test test/*.test.cjs",
    "posttest": "npm run package"
  },
  "keywords": [
    "github-actions",
    "oidc",
    "datarecs",
    "authentication"
  ],
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1"
  },
  "overrides": {
    "undici": "^6.28.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3"
  }
}
